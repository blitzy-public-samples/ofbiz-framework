<?xml version="1.0" encoding="UTF-8" ?>
<!--
Licensed to the Apache Software Foundation (ASF) under one
or more contributor license agreements.  See the NOTICE file
distributed with this work for additional information
regarding copyright ownership.  The ASF licenses this file
to you under the Apache License, Version 2.0 (the
"License"); you may not use this file except in compliance
with the License.  You may obtain a copy of the License at

http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing,
software distributed under the License is distributed on an
"AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
KIND, either express or implied.  See the License for the
specific language governing permissions and limitations
under the License.
-->
<!--
 - This file configures the Entity Engine JDBC (or other DataSource) and JTA
access. For a detailed description see the core/docs/entityconfig.html file.
-->
<entity-config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="https://ofbiz.apache.org/dtds/entity-config.xsd">
    <resource-loader name="fieldfile" class="org.apache.ofbiz.base.config.FileLoader"
            prepend-env="ofbiz.home" prefix="/framework/entity/fieldtype/"/>

    <!-- the transaction factory class to use, one is needed for each way of getting JTA interfaces -->
    <!-- Use this one for Geronimo -->
    <transaction-factory class="org.apache.ofbiz.entity.transaction.GeronimoTransactionFactory"/>

    <!-- Use this one for getting the JTA objects from JNDI -->
    <!-- NOTE: to use the JndiFactory you must specify the necessary JNDI properties
    <transaction-factory class="org.apache.ofbiz.entity.transaction.JNDITransactionFactory">
        <user-transaction-jndi jndi-server-name="default" jndi-name="java:comp/UserTransaction"/>
        <transaction-manager-jndi jndi-server-name="default" jndi-name="java:comp/UserTransaction"/>
    </transaction-factory>
    -->
    <!--
        It is common to use UserTransaction for the TransactionManager, but if that doesn't work, try this: <transaction-manager-jndi jndi-server-name="default" jndi-name="java:comp/TransactionManager"/>
        Common UserTransaction locations:
            java:comp/UserTransaction (most servers: Resin, Orion, OC4J, etc)
            UserTransaction (RexIP)
        JBoss uses two different objects for the UserTransaction and TransactionManager interfaces;
        they are located in JNDI at: "java:comp/UserTransaction" and "java:/TransactionManager" respectively
    -->

    <!-- the connection factory class to use, one is needed for obtaining connections/pools for defined resources -->
    <connection-factory class="org.apache.ofbiz.entity.connection.DBCPConnectionFactory"/>

    <debug-xa-resources value="false" />  <!-- see https://issues.apache.org/jira/browse/OFBIZ-4282 for more -->
    
    <!-- Deployment profiles (see DOCKER.adoc):
         * Development profile (this committed file, zero configuration): the default, default-no-eca and test
           delegators all resolve to the embedded H2 datasources, so a bare checkout boots with no environment
           variables and no secrets configured. This is the profile every local build and every gradlew task uses.
         * Deployed profile (managed PostgreSQL): the container entry point renders
           docker/templates/postgres-entityengine.xml into /ofbiz/config/entityengine.xml (which precedes
           ofbiz.jar on the classpath) when OFBIZ_POSTGRES_HOST is set, repointing the default and
           default-no-eca group-maps to localpostgres / localpostgresolap / localpostgrestenant. Connection
           parameters come from OFBIZ_POSTGRES_HOST, OFBIZ_POSTGRES_PORT (default 5432),
           OFBIZ_POSTGRES_{OFBIZ,OLAP,TENANT}_{DB,USER,PASSWORD}, OFBIZ_DB_POOL_MIN (default 2) and
           OFBIZ_DB_POOL_MAX (default 250); the two startup-DDL flags come from OFBIZ_SCHEMA_INIT and the
           cache-invalidation flag from OFBIZ_DISTRIBUTED_CACHE_CLEAR, both defaulting to false - see the
           cache-invalidation note immediately below and the startup-DDL note above the localpostgres
           datasource.
           Every one of those values is validated before it is substituted, and the entry point then re-reads
           the rendered file and refuses to start if a security-relevant substitution did not take effect, so a
           stale template fails the start up instead of quietly weakening the configuration.
         * Development profile with cross-instance cache invalidation: OFBIZ_DISTRIBUTED_CACHE_CLEAR=true
           without OFBIZ_POSTGRES_HOST renders THIS file into /ofbiz/config/entityengine.xml with nothing but
           the two cache-clear attributes rewritten, so the behaviour can be exercised on embedded H2.
         The test delegator ALWAYS stays on embedded H2 in both profiles: the deployed-profile template defines
         its own localh2* datasources for exactly that reason and the entry point verifies the rendered mapping,
         so gradlew loadAll and gradlew testIntegration are unaffected, in a container as well as locally.
         H2, MySQL and Oracle datasource definitions remain fully intact and selectable. -->
    <!-- distributed-cache-clear-enabled is configuration-driven and stays "false" here, so an unconfigured
         checkout keeps single-node cache behaviour. OFBIZ_DISTRIBUTED_CACHE_CLEAR=true makes the container entry
         point render this attribute, and the matching one on default-no-eca, as "true" - by substituting the
         placeholder in docker/templates/postgres-entityengine.xml in the deployed profile, or by rewriting these
         two attributes of this file when no managed database is configured. Only these two delegators are
         rewritten, and the entry point re-reads the rendered file and refuses to start unless both carry the
         requested value and "test" carries no such attribute at all. The delegator then uses the
         DistributedCacheClear defaults, org.apache.ofbiz.entityext.cache.EntityCacheServices running as the
         "system" user, so no further attribute is needed here.
         A MESSAGE TRANSPORT IS MANDATORY: the distributedClear* sender services in
         framework/entityext/servicedef/services.xml are declared engine="jms" location="serviceMessenger", the
         jms-service of that name is commented out in framework/service/config/serviceengine.xml, no JMS provider
         client library is bundled, and those services inherit use-transaction true with require-new-transaction
         false, so a dispatch failure marks the caller's transaction rollback only and loses the entity write that
         triggered the invalidation.
         docker/docker-entrypoint.sh therefore treats the transport as part of enabling the flag: whenever the flag
         resolves to true it renders an active serviceMessenger into /ofbiz/config/serviceengine.xml from the
         OFBIZ_JMS_* variables, reads every attribute back out of the installed file and compares it with what was
         requested, asserts that exactly one serviceMessenger is active, and requires that the rendered
         jndi.properties declares the connection factory and topic names while leaving the two JVM-wide keys
         java.naming.factory.initial and java.naming.provider.url exactly as shipped. It then requires three
         pieces of start up evidence: the active element declares listen="true", so this instance subscribes
         rather than only publishing; the initial-context-factory class named by the jndi-server resolves on the
         class path, which is where an absent /ofbiz/lib-extra client library is caught by name; and at least one
         host and port in the provider URL accepts a TCP connection within a bounded deadline.
         WHAT THAT DOES NOT ESTABLISH, and why none of it may be read as proof of cache coherence: no JNDI context
         is created, the connection factory and topic names are checked only for being DECLARED and are never
         looked up, no credentials are authenticated, no subscriber or publisher is constructed, and nothing is
         published - so delivery between instances is not verified at start up. The three checks are necessary
         conditions, not sufficient ones. A live lookup is not available to this path at all: dependencies.gradle
         bundles only the JMS API, the provider's client library is deployment specific and supplied by the
         operator, so there is nothing to perform the lookup with when the configuration is rendered. Coherence is
         established by running two instances and observing an invalidation arrive, for which DOCKER.adoc carries
         the procedure; while an instance runs, its subscriber's own connection state is the second readiness
         dimension of /webtools/health/ready, which is where a broker that goes away is reported. Any failure of
         the three aborts the start up under both OFBIZ_PROFILE=dev and OFBIZ_PROFILE=prod. DOCKER.adoc carries the
         operator procedure for supplying the provider client library and the JNDI settings. -->
    <delegator name="default" entity-model-reader="main" entity-group-reader="main" entity-eca-reader="main" distributed-cache-clear-enabled="false">
        <group-map group-name="org.apache.ofbiz" datasource-name="localh2"/>
        <group-map group-name="org.apache.ofbiz.olap" datasource-name="localh2olap"/>
        <group-map group-name="org.apache.ofbiz.tenant" datasource-name="localh2tenant"/>
    </delegator>
    <!-- May be used when you create a service that manages many data for massive imports, this for performance reason or to escape functional cases --> 
    <delegator name="default-no-eca" entity-model-reader="main" entity-group-reader="main" entity-eca-reader="main" entity-eca-enabled="false" distributed-cache-clear-enabled="false">
        <group-map group-name="org.apache.ofbiz" datasource-name="localh2"/>
        <group-map group-name="org.apache.ofbiz.olap" datasource-name="localh2olap"/>
        <group-map group-name="org.apache.ofbiz.tenant" datasource-name="localh2tenant"/>
    </delegator>

    <!-- Be sure that your default delegator (or the one you use) uses the same datasource for test. You must run "gradlew loadAll" before running "gradlew testIntegration" -->
    <delegator name="test" entity-model-reader="main" entity-group-reader="main" entity-eca-reader="main">
        <group-map group-name="org.apache.ofbiz" datasource-name="localh2"/>
        <group-map group-name="org.apache.ofbiz.olap" datasource-name="localh2olap"/>
        <group-map group-name="org.apache.ofbiz.tenant" datasource-name="localh2tenant"/>
    </delegator>
   
    <!-- need to at least define a name for each component to use -->
    <entity-model-reader name="main"/>

    <!-- need to at least define a name for each component to use -->
    <entity-group-reader name="main"/>

    <!-- need to at least define a name for each component to use -->
    <entity-eca-reader name="main"/>

    <!-- need to at least define a name for each component to use -->
    <!-- defining:
        tenant       = OFBiz and External Tenant Data
        seed         = OFBiz and External Seed Data - to be maintained along with source and updated whenever a system deployment is updated
        seed-initial = OFBiz and External Seed Data - to be maintained along with source like other seed data, but only loaded initially and not updated when a system is updated except manually reviewing each line
        demo         = OFBiz Only Demo Data
        ext          = External General Data (custom)
        ext-test     = External Test Data (custom)
        ext-demo     = External Demo Data (custom)
    -->
    <entity-data-reader name="tenant"/>
    <entity-data-reader name="seed"/>
    <entity-data-reader name="seed-initial"/>
    <entity-data-reader name="demo"/>
    <entity-data-reader name="ext"/>
    <entity-data-reader name="ext-test"/>
    <entity-data-reader name="ext-demo"/>

    <field-type name="h2" loader="fieldfile" location="fieldtypeh2.xml"/>
    <field-type name="mysql" loader="fieldfile" location="fieldtypemysql.xml"/>
    <field-type name="postgres" loader="fieldfile" location="fieldtypepostgres.xml"/>
    <field-type name="oracle" loader="fieldfile" location="fieldtypeoracle.xml"/>
    <field-type name="sapdb" loader="fieldfile" location="fieldtypesapdb.xml"/>
    <field-type name="sybase" loader="fieldfile" location="fieldtypesybase.xml"/>
    <field-type name="firebird" loader="fieldfile" location="fieldtypefirebird.xml"/>
    <field-type name="mssql" loader="fieldfile" location="fieldtypemssql.xml"/>
    <field-type name="advantage" loader="fieldfile" location="fieldtypeadvantage.xml"/>
    <field-type name="hsql" loader="fieldfile" location="fieldtypehsql.xml"/>
    <field-type name="daffodil" loader="fieldfile" location="fieldtypedaffodil.xml"/>
    <field-type name="axion" loader="fieldfile" location="fieldtypeaxion.xml"/>

    <!--
    For DAO/JDBC Helper: Tries:
      1. JNDI Datasource IF jdbc.jndi.name, context.provider, etc are specified
      2. Embedded (JOTM) if available (uses jdbc.driver, jdbc.uri, jdbc.username, jdbc.password, isolation.level)
      3. Direct to manually loaded JDBC driver (uses jdbc.driver, jdbc.uri, jdbc.username, jdbc.password)

    Transaction Isolation Levels - (<helperName>.isolation.level) can be as follows:
     * None
     * ReadCommitted (Recommended)
     * ReadUncommitted
     * RepeatableRead
     * Serializable (NOT Recommended)
     * Not set uses database default
    -->

    <datasource name="localh2"
            helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
            schema-name="OFBIZ"
            field-type-name="h2"
            check-on-start="true"
            add-missing-on-start="true"
            max-worker-pool-size="1"
            use-pk-constraint-names="false"
            use-indices-unique="false"
            alias-view-columns="false"
            use-order-by-nulls="true"
            offset-style="fetch">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
        <inline-jdbc
                jdbc-driver="org.h2.Driver"
                jdbc-uri="jdbc:h2:file:./runtime/data/h2/ofbiz;DB_CLOSE_ON_EXIT=FALSE;CACHE_SIZE=65536;INIT=CREATE SCHEMA IF NOT EXISTS OFBIZ\;SET SCHEMA OFBIZ"
                jdbc-username="ofbiz"
                jdbc-password-lookup="h2-ofbiz"
                isolation-level="ReadCommitted"
                pool-minsize="2"
                pool-maxsize="250"
                pool-sleeptime="300000"
                test-on-borrow="true"
                pool-jdbc-test-stmt="SELECT 1"
                soft-min-evictable-idle-time-millis="600000"
                time-between-eviction-runs-millis="600000"/>
        <!-- <jndi-jdbc jndi-server-name="localjndi" jndi-name="java:/H2DataSource" isolation-level="ReadCommitted"/> -->
    </datasource>
    <datasource name="localh2odbc"
        helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
        schema-name="ODBC"
        field-type-name="h2"
        check-on-start="true"
        add-missing-on-start="true"
        max-worker-pool-size="1"
        use-pk-constraint-names="false"
        use-indices-unique="false"
        alias-view-columns="false"
        use-order-by-nulls="true"
        offset-style="fetch">
        <inline-jdbc
            jdbc-driver="org.h2.Driver"
            jdbc-uri="jdbc:h2:file:./runtime/data/h2/ofbiz;DB_CLOSE_ON_EXIT=FALSE;CACHE_SIZE=65536;INIT=CREATE SCHEMA IF NOT EXISTS ODBC\;SET SCHEMA ODBC"
            jdbc-username="ofbiz"
            jdbc-password-lookup="h2-ofbizodbc"
            isolation-level="ReadCommitted"
            pool-minsize="2"
            pool-maxsize="250"
            pool-sleeptime="300000"
            test-on-borrow="true"
            pool-jdbc-test-stmt="SELECT 1"
            time-between-eviction-runs-millis="600000"/>
        <!-- <jndi-jdbc jndi-server-name="localjndi" jndi-name="java:/H2DataSource" isolation-level="ReadCommitted"/> -->
    </datasource>
    <datasource name="localh2olap"
            helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
            schema-name="OLAP"
            field-type-name="h2"
            check-on-start="true"
            add-missing-on-start="true"
            max-worker-pool-size="1"
            use-pk-constraint-names="false"
            use-indices-unique="false"
            alias-view-columns="false"
            use-order-by-nulls="true">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
        <inline-jdbc
                jdbc-driver="org.h2.Driver"
                jdbc-uri="jdbc:h2:file:./runtime/data/h2/ofbiz;DB_CLOSE_ON_EXIT=FALSE;CACHE_SIZE=65536;INIT=CREATE SCHEMA IF NOT EXISTS OLAP\;SET SCHEMA OLAP"
                jdbc-username="ofbiz"
                jdbc-password-lookup="h2-ofbizolap"
                isolation-level="ReadCommitted"
                pool-minsize="2"
                pool-maxsize="250"
                pool-sleeptime="300000"
                test-on-borrow="true"
                pool-jdbc-test-stmt="SELECT 1"
                time-between-eviction-runs-millis="600000"/>
        <!-- <jndi-jdbc jndi-server-name="localjndi" jndi-name="java:/H2DataSource" isolation-level="ReadCommitted"/> -->
    </datasource>
    <datasource name="localh2tenant"
        helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
        schema-name="TENANT"
        field-type-name="h2"
        check-on-start="true"
        add-missing-on-start="true"
        max-worker-pool-size="1"
        use-pk-constraint-names="false"
        use-indices-unique="false"
        alias-view-columns="false"
        use-order-by-nulls="true">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
        <inline-jdbc
            jdbc-driver="org.h2.Driver"
            jdbc-uri="jdbc:h2:file:./runtime/data/h2/ofbiz;DB_CLOSE_ON_EXIT=FALSE;CACHE_SIZE=65536;INIT=CREATE SCHEMA IF NOT EXISTS TENANT\;SET SCHEMA TENANT"
            jdbc-username="ofbiz"
            jdbc-password-lookup="h2-ofbiztenant"
            isolation-level="ReadCommitted"
            pool-minsize="2"
            pool-maxsize="250"
            pool-sleeptime="300000"
            test-on-borrow="true"
            pool-jdbc-test-stmt="SELECT 1"
            time-between-eviction-runs-millis="600000"/>
        <!-- <jndi-jdbc jndi-server-name="localjndi" jndi-name="java:/H2DataSource" isolation-level="ReadCommitted"/> -->
    </datasource>

    <!-- Also consider:
    mysql.conf
    [client]
    default-character-set=utf8
    [mysql]
    default-character-set=utf8
    [mysqld]
    collation-server = utf8_unicode_ci
    init-connect='SET NAMES utf8'
    character-set-server = utf8
    
    Mysql Version: > 5.6.4 (supports datetime milliseconds)
     -->
    <datasource name="localmysql"
            helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
            field-type-name="mysql"
            check-on-start="true"
            add-missing-on-start="true"
            check-pks-on-start="false"
            use-foreign-keys="true"
            join-style="ansi-no-parenthesis"
            alias-view-columns="false"
            drop-fk-use-foreign-key-keyword="true"
            table-type="InnoDB"
            character-set="utf8"
            collate="utf8_general_ci"
            offset-style="limit">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
        <inline-jdbc
                jdbc-driver="com.mysql.cj.jdbc.Driver"
                jdbc-uri="jdbc:mysql://127.0.0.1/ofbiz?autoReconnect=true&amp;characterEncoding=UTF-8"
                jdbc-username="ofbiz"
                jdbc-password="ofbiz"
                isolation-level="ReadCommitted"
                pool-minsize="2"
                pool-maxsize="250"
                time-between-eviction-runs-millis="600000"/><!-- Please note that at least one person has experienced a problem with this value with MySQL
                and had to set it to -1 in order to avoid this issue.
                For more look at http://markmail.org/thread/5sivpykv7xkl66px and http://commons.apache.org/dbcp/configuration.html-->
        <!-- <jndi-jdbc jndi-server-name="localjndi" jndi-name="java:/MySqlDataSource" isolation-level="Serializable"/> -->
    </datasource>
    <datasource name="localmysqlolap"
            helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
            field-type-name="mysql"
            check-on-start="true"
            add-missing-on-start="true"
            check-pks-on-start="false"
            use-foreign-keys="true"
            join-style="ansi-no-parenthesis"
            alias-view-columns="false"
            drop-fk-use-foreign-key-keyword="true"
            table-type="InnoDB"
            character-set="utf8"
            collate="utf8_general_ci"
            offset-style="limit">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
        <inline-jdbc
                jdbc-driver="com.mysql.cj.jdbc.Driver"
                jdbc-uri="jdbc:mysql://127.0.0.1/ofbizolap?autoReconnect=true&amp;characterEncoding=UTF-8"
                jdbc-username="ofbiz"
                jdbc-password="ofbiz"
                isolation-level="ReadCommitted"
                pool-minsize="2"
                pool-maxsize="250"
                time-between-eviction-runs-millis="600000"/><!-- Please note that at least one person has experienced a problem with this value with MySQL
                and had to set it to -1 in order to avoid this issue.
                For more look at http://markmail.org/thread/5sivpykv7xkl66px and http://commons.apache.org/dbcp/configuration.html-->
        <!-- <jndi-jdbc jndi-server-name="localjndi" jndi-name="java:/MySqlDataSource" isolation-level="Serializable"/> -->
    </datasource>
    <datasource name="localmysqltenant"
            helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
            field-type-name="mysql"
            check-on-start="true"
            add-missing-on-start="true"
            check-pks-on-start="false"
            use-foreign-keys="true"
            join-style="ansi-no-parenthesis"
            alias-view-columns="false"
            drop-fk-use-foreign-key-keyword="true"
            table-type="InnoDB"
            character-set="utf8"
            collate="utf8_general_ci"
            offset-style="limit">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
        <inline-jdbc
                jdbc-driver="com.mysql.cj.jdbc.Driver"
                jdbc-uri="jdbc:mysql://127.0.0.1/ofbiztenant?autoReconnect=true&amp;characterEncoding=UTF-8"
                jdbc-username="ofbiz"
                jdbc-password="ofbiz"
                isolation-level="ReadCommitted"
                pool-minsize="2"
                pool-maxsize="250"
                time-between-eviction-runs-millis="600000"/><!-- Please note that at least one person has experienced a problem with this value with MySQL
                and had to set it to -1 in order to avoid this issue.
                For more look at http://markmail.org/thread/5sivpykv7xkl66px and http://commons.apache.org/dbcp/configuration.html-->
        <!-- <jndi-jdbc jndi-server-name="localjndi" jndi-name="java:/MySqlDataSource" isolation-level="Serializable"/> -->
    </datasource>
    <datasource name="odbcmysql"
            helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
            field-type-name="mysql"
            check-on-start="true"
            add-missing-on-start="true"
            check-pks-on-start="false"
            use-foreign-keys="true"
            join-style="ansi-no-parenthesis"
            alias-view-columns="false"
            drop-fk-use-foreign-key-keyword="true"
            table-type="InnoDB"
            character-set="utf8"
            collate="utf8_general_ci"
            offset-style="limit">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <inline-jdbc
                jdbc-driver="com.mysql.cj.jdbc.Driver"
                jdbc-uri="jdbc:mysql://127.0.0.1/ofbiz_odbc?autoReconnect=true&amp;characterEncoding=UTF-8"
                jdbc-username="ofbiz"
                jdbc-password="ofbiz"
                isolation-level="ReadCommitted"
                pool-minsize="2"
                pool-maxsize="250"
                time-between-eviction-runs-millis="600000"/>
        <!-- <jndi-jdbc jndi-server-name="localjndi" jndi-name="java:/MySqlDataSource" isolation-level="Serializable"/> -->
    </datasource>

    <!-- Objective 1 (managed PostgreSQL datasource): the three localpostgres* definitions below are the
         deployed-profile datasources. The Entity Engine configuration parser reads these attributes LITERALLY;
         there is no property-placeholder or system-property expansion anywhere in this file, so externalization
         is performed by the container entry point, which renders
         docker/templates/postgres-entityengine.xml into /ofbiz/config/entityengine.xml (first on the runtime
         classpath) when OFBIZ_POSTGRES_HOST is set. Contract with that template - committed value on the left,
         the environment variable the template's corresponding placeholder is filled from on the right. The
         placeholder NAMES are deliberately not written out here; see the template, which documents each one at
         the attribute it occupies. The entry point refuses to render a template whose placeholder census
         differs from this contract, and EntityEngineConfigContractTests asserts the same census from the
         build, so the two artifacts cannot drift apart silently:
           jdbc-uri            jdbc:postgresql://127.0.0.1:5432/ofbiz?sslmode=verify-full&amp;connectTimeout=10
                               &amp;socketTimeout=60&amp;loginTimeout=30&amp;cancelSignalTimeout=10&amp;tcpKeepAlive=true
                               ..  host from OFBIZ_POSTGRES_HOST, port from OFBIZ_POSTGRES_PORT (default 5432),
                               database from OFBIZ_POSTGRES_OFBIZ_DB, the whole TLS query string from
                               OFBIZ_POSTGRES_SSLMODE (default verify-full) and OFBIZ_POSTGRES_SSLROOTCERT, and
                               the network deadlines from OFBIZ_POSTGRES_{CONNECT,SOCKET,LOGIN,CANCEL,QUERY}_TIMEOUT
                               and OFBIZ_POSTGRES_TCP_KEEPALIVE - see the deadline note below
           jdbc-username       ofbiz   ..  from OFBIZ_POSTGRES_OFBIZ_USER
           jdbc-password       omitted ..  from OFBIZ_POSTGRES_OFBIZ_PASSWORD, which the entry point REQUIRES
                               with no default at all - see the fail-closed note below
           pool-minsize        2       ..  from OFBIZ_DB_POOL_MIN
           pool-maxsize        250     ..  from OFBIZ_DB_POOL_MAX
           pool-sleeptime      20000   ..  from OFBIZ_DB_POOL_WAIT
           test-on-borrow      false   ..  from OFBIZ_DB_POOL_TEST_ON_BORROW
           pool-jdbc-test-stmt SELECT 1  ..  the same literal; a fixed validation query, not configurable
           test-while-idle     true    ..  the same literal; idle validation is always on
           check-on-start      false   ..  rendered true in init mode, false in run mode, from OFBIZ_SCHEMA_INIT
           add-missing-on-start  false ..  rendered the same way from OFBIZ_SCHEMA_INIT, through a placeholder of
                               its own rather than one shared with check-on-start, because the two attributes are
                               parsed by different rules and cannot share a single rendered literal safely
           cache clear flag    false   ..  rendered from OFBIZ_DISTRIBUTED_CACHE_CLEAR, on the two default
                               delegators only and never on test
           test delegator      localh2 datasources  ..  stays on those same localh2* datasources in the template,
                               which now defines them itself, so gradlew loadAll and gradlew testIntegration keep
                               running against embedded H2 inside a container as well
         MAINTENANCE RULE: never write an at-sign delimited placeholder name anywhere in THIS file, not even in a
         comment. This file is itself a sed source - the entry point renders it into /ofbiz/config/entityengine.xml
         to rewrite the cache-clear attribute when cross-instance invalidation is requested without a managed
         database - and sed rewrites comments as readily as attributes. A placeholder named here would therefore be
         substituted in the prose too, and one that is never substituted would survive into the rendered file as a
         literal, which the entry point refuses outright: every rendered configuration is rejected when any
         placeholder survives substitution, so a placeholder quoted in prose here would fail that check on a file
         that is in fact fully rendered. The same rule applies to the template for the stronger reason that its
         placeholders carry the database passwords: a secret-bearing placeholder named in a comment there would
         republish the password in clear text, which is why the entry point refuses to render a template that
         names a placeholder inside a comment at all, secret bearing or not. There is a second, purely
         syntactic reason for the same rule: a substituted value containing a double hyphen would turn the
         surrounding comment into malformed XML. Describe placeholders in prose by the attribute they
         occupy, as the comments here do.
         The explicit :5432 port below is the PostgreSQL default, so this is a functional no op for anyone already
         using these definitions, while giving the deployed profile an unambiguous host:port substitution shape:
         the port is a placeholder of its own, range checked as an integer between 1 and 65535, and a host value
         that carries an embedded port is refused, so no part of the rendered URI reaches it unvalidated.
         The olap and tenant datasources follow the same pattern, from OFBIZ_POSTGRES_OLAP_* and
         OFBIZ_POSTGRES_TENANT_*.
         NO COMMITTED PASSWORD, FAIL CLOSED: these three definitions carry no jdbc-password at all. jdbc-password
         is optional in entity-config.xsd while jdbc-username is required, so the long standing ofbiz username
         default for a locally installed PostgreSQL is kept (a username on its own cannot authenticate) and the
         password is replaced by an explicit jdbc-password-lookup. EntityConfig.getJdbcPassword resolves that
         lookup against framework/base/config/passwords.properties, which deliberately contains NO
         jdbc-password.localpostgres* entry, so opening one of these connections without externalized credentials
         throws GenericEntityConfException naming the missing property rather than silently authenticating with a
         password that is public in the source tree. Supply the password through the deployed profile (the template
         token above), or, for a local PostgreSQL, add the jdbc-password.localpostgres* property to a
         passwords.properties that is not committed. The application's other secrets are configured through
         framework/security/config/security.properties and
         framework/start/src/main/resources/org/apache/ofbiz/base/start/start.properties, which ship without a
         value and are injected from the environment at deploy time; none of them belongs here.
         TLS REQUIRED AND VERIFIED, NOT PREFERRED: every managed URI below pins sslmode=verify-full, which encrypts
         the connection, checks that the server certificate chains to a trusted root AND checks that the hostname
         matches that certificate. It is spelled out rather than left to the driver because pgJDBC's own default,
         sslmode=prefer, silently falls back to an UNENCRYPTED connection when the server declines TLS, and
         sslmode=require encrypts without authenticating the peer, so a man in the middle can present any
         certificate: both leave the database password above - and every entity row after it - readable or
         forgeable by anyone on the path between the fleet and the managed database. An absent query string is
         therefore not a neutral default, it is the weakest of the six modes.
         verify-full needs a trusted root, and pgJDBC does NOT consult the JVM trust store by default: it reads
         sslrootcert, which defaults to ${user.home}/.postgresql/root.crt. A deployment therefore either mounts its
         provider CA bundle and points OFBIZ_POSTGRES_SSLROOTCERT at it, or imports the CA into the JVM trust store
         and appends sslfactory=org.postgresql.ssl.DefaultJavaSSLFactory.
         OFBIZ_POSTGRES_SSLMODE can relax the mode for a development database that has no TLS listener - the whole
         query string above is substituted as a single unit, so the rendered URI stays well formed with or without a
         trusted root path - but the entry point refuses every non-verifying mode when OFBIZ_PROFILE=prod, and it
         re-reads the rendered file and refuses to start unless every managed URI in it really carries the resolved
         mode. The deployed profile therefore cannot be downgraded to plaintext, whether by an environment variable
         or by a template that predates the substitution.
         EVERY NETWORK WAIT IS BOUNDED: each managed URI below also pins the pgJDBC deadlines, and each managed
         pool below pins its borrow wait, because in both cases the value that applies when the setting is absent
         is not a deadline at all. pgJDBC leaves socketTimeout and loginTimeout at 0, which means NO LIMIT, so a
         thread that reaches a database which has stopped answering - a managed instance that failed over, a
         dropped NAT mapping, a network partition - blocks on the socket read for as long as the kernel keeps the
         connection open. That is not confined to request threads: the same block strands the startup entity check
         and the readiness probe's query, which is what makes an instance that is merely waiting
         indistinguishable from one that is wedged. Independently, InlineJdbc reads an absent pool-sleeptime as
         300000 ms, five minutes, which is longer than any load-balancer health-check timeout, so an exhausted
         pool presented as a hung instance rather than as pool exhaustion.
         socketTimeout bounds a SINGLE socket read rather than a statement, so the value is deliberately generous
         enough that no normal statement, data load or schema check is affected while still being finite - it is
         what lets a stalled readiness query eventually return so readiness recovers without a restart. There is
         deliberately no queryTimeout here: it would apply to EVERY statement and abort legitimately long work
         such as a data load or a large report, so it stays off unless a deployment sets
         OFBIZ_POSTGRES_QUERY_TIMEOUT, which the entry point then renders into the URI.
         pool-jdbc-test-stmt with test-while-idle stops the pool handing out a connection that died while idle,
         the ordinary outcome of a managed-database failover; without a validation query DBCP performs no
         validation at all, whichever of the test-* flags is set, and a borrowing thread would then block on a
         dead socket until socketTimeout expired. Validating during the eviction runs that already happen every
         time-between-eviction-runs-millis costs a request nothing, so test-on-borrow - one extra round trip on
         every single borrow - stays false and is opt-in through OFBIZ_DB_POOL_TEST_ON_BORROW.
         These committed literals match the entry point's own defaults, so a deployment that renders the template
         and one that uses these definitions directly - a non-container install pointing the default delegator at
         localpostgres - are bounded identically. Only the three managed localpostgres* definitions carry them:
         the MySQL, Oracle and other dialect definitions are preserved exactly as they are for portability.
         RELAXING IT, AND ENFORCING IT FROM THE SERVER: because this file is read literally, the mode pinned above
         cannot be relaxed from the environment; only the rendered deployed profile can, through
         OFBIZ_POSTGRES_SSLMODE. A non-container installation that points a delegator at these definitions against
         an ordinary local PostgreSQL, which ships with ssl=off, therefore edits the URI in its own copy of this
         file, the same copy it must already extend with the jdbc-password.localpostgres* property described
         above, since these definitions deliberately carry no password. The two levers cover different things
         and neither substitutes for the other: a hostssl rule in pg_hba.conf, or rds.force_ssl on a managed
         instance, makes the SERVER refuse an unencrypted client and cannot be forgotten the way a per-client
         setting can, while the client sslmode is what decides whether this instance verifies the server's
         certificate and hostname. A deployment that requires encrypted, verified connections sets both. Note also that pgJDBC ignores libpq's PGSSLMODE and
         PGSSLROOTCERT environment variables, so neither the mode nor the trusted root can be supplied that
         way. A deployment that needs pgJDBC TLS properties this file does not parameterise supplies its own
         override instead: mount an entityengine.xml under /ofbiz/config and leave OFBIZ_POSTGRES_HOST unset,
         which suppresses the template while keeping the mounted file first on the classpath. -->
    <!-- Objective 4 (gated schema initialization): this managed-RDBMS run mode issues NO startup DDL, so a
         serving instance needs no DDL privileges and a whole fleet can scale out without racing on schema
         changes. Schema changes are applied exclusively by a separate one-shot init execution driven by
         OFBIZ_SCHEMA_INIT=true, which the container entry point runs as three rendered flag pairs:
         check-on-start and add-missing-on-start both true for the additive pass (it creates missing tables
         and columns and never drops), then check-on-start true with add-missing-on-start false for a second,
         non-amending pass that asks the database itself rather than the log whether anything is still
         missing, then both false again before the process exits 0 without ever reaching the serving command
         - so the DDL privilege that execution needs never belongs to a long-lived process. Each pass is
         judged on the loader's exit status, a completion signature in its output and a finite set of known
         failure and discrepancy signatures, because the loader logs entity-model discrepancies as warnings
         and still exits zero; success is therefore only reported after the DDL has actually been attempted
         and verified. A data load of "none" still invokes the ofbiz loader with the option "readers=none",
         because creating the delegator is what applies the schema and an empty reader list then loads
         nothing, and OFBIZ_SCHEMA_INIT=true combined with OFBIZ_SKIP_INIT is refused outright rather than
         exiting successfully having created nothing. After each render the entry point re-reads the file and
         refuses to continue unless all three managed datasources agree on the requested mode, so neither a
         stale template nor a hand-edited copy can leave a serving instance issuing DDL.
         Note how Datasource.java parses the two flags: check-on-start is read as
         !"false".equals(attribute), so an absent attribute means TRUE and the literal false below is what
         actually disables the startup schema check, while add-missing-on-start is read as
         "true".equals(attribute), so its false is explicit rather than required.
         Schema truth remains the entity model - no Flyway, Liquibase, schema-version table or hand-written DDL.
         The embedded localh2* datasources deliberately keep both flags enabled for zero-configuration local
         development, where the file-based database belongs to the single JVM that opens it. -->
    <datasource name="localpostgres"
            helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
            schema-name="public"
            field-type-name="postgres"
            check-on-start="false"
            add-missing-on-start="false"
            use-fk-initially-deferred="false"
            alias-view-columns="false"
            join-style="ansi"
            use-binary-type-for-blob="true"
            use-order-by-nulls="true"
            offset-style="limit"
            result-fetch-size="50"> <!-- Comment out the result-fetch-size attribute for jdbc driver versions older than 8.0.
            Not recommended to use those though. They are archived unsupported versions: http://jdbc.postgresql.org/download.html -->
        
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
        <inline-jdbc
                jdbc-driver="org.postgresql.Driver"
                jdbc-uri="jdbc:postgresql://127.0.0.1:5432/ofbiz?sslmode=verify-full&amp;connectTimeout=10&amp;socketTimeout=60&amp;loginTimeout=30&amp;cancelSignalTimeout=10&amp;tcpKeepAlive=true"
                jdbc-username="ofbiz"
                jdbc-password-lookup="localpostgres"
                isolation-level="ReadCommitted"
                pool-minsize="2"
                pool-maxsize="250"
                time-between-eviction-runs-millis="600000"
                pool-sleeptime="20000"
                pool-jdbc-test-stmt="SELECT 1"
                test-while-idle="true"
                test-on-borrow="false"/><!-- Be warned that at this date (2009-09-20) the max_connections parameters in postgresql.conf
                is set by default to 100 by the initdb process see http://www.postgresql.org/docs/8.4/static/runtime-config-connection.html#GUC-MAX-CONNECTIONS-->

        <!-- <jndi-jdbc jndi-server-name="default" jndi-name="java:comp/env/jdbc/localpostgres" isolation-level="ReadCommitted"/>-->
        <!-- <jndi-jdbc jndi-server-name="default" jndi-name="comp/env/jdbc/xa/localpostgres" isolation-level="ReadCommitted"/> --> <!-- Orion Style JNDI name -->
        <!-- <jndi-jdbc jndi-server-name="localweblogic" jndi-name="PostgresDataSource"/> --> <!-- Weblogic Style JNDI name -->
        <!-- <jndi-jdbc jndi-server-name="default" jndi-name="jdbc/localpostgres" isolation-level="ReadCommitted"/> --> <!-- JRun4 Style JNDI name -->
        <!-- <tyrex-dataSource dataSource-name="localpostgres" isolation-level="ReadCommitted"/> -->
    </datasource>
    <datasource name="localpostgresolap"
            helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
            schema-name="public"
            field-type-name="postgres"
            check-on-start="false"
            add-missing-on-start="false"
            use-fk-initially-deferred="false"
            alias-view-columns="false"
            join-style="ansi"
            result-fetch-size="50"
            use-binary-type-for-blob="true"
            use-order-by-nulls="true"
            offset-style="limit">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
        <inline-jdbc
                jdbc-driver="org.postgresql.Driver"
                jdbc-uri="jdbc:postgresql://127.0.0.1:5432/ofbizolap?sslmode=verify-full&amp;connectTimeout=10&amp;socketTimeout=60&amp;loginTimeout=30&amp;cancelSignalTimeout=10&amp;tcpKeepAlive=true"
                jdbc-username="ofbiz"
                jdbc-password-lookup="localpostgresolap"
                isolation-level="ReadCommitted"
                pool-minsize="2"
                pool-maxsize="250"
                time-between-eviction-runs-millis="600000"
                pool-sleeptime="20000"
                pool-jdbc-test-stmt="SELECT 1"
                test-while-idle="true"
                test-on-borrow="false"/><!-- Be warned that at this date (2009-09-20) the max_connections parameters in postgresql.conf
                    is set by default to 100 by the initdb process see http://www.postgresql.org/docs/8.4/static/runtime-config-connection.html#GUC-MAX-CONNECTIONS-->
        
        <!-- <jndi-jdbc jndi-server-name="default" jndi-name="java:comp/env/jdbc/localpostgres" isolation-level="ReadCommitted"/>-->
        <!-- <jndi-jdbc jndi-server-name="default" jndi-name="comp/env/jdbc/xa/localpostgres" isolation-level="ReadCommitted"/> --> <!-- Orion Style JNDI name -->
        <!-- <jndi-jdbc jndi-server-name="localweblogic" jndi-name="PostgresDataSource"/> --> <!-- Weblogic Style JNDI name -->
        <!-- <jndi-jdbc jndi-server-name="default" jndi-name="jdbc/localpostgres" isolation-level="ReadCommitted"/> --> <!-- JRun4 Style JNDI name -->
        <!-- <tyrex-dataSource dataSource-name="localpostgres" isolation-level="ReadCommitted"/> -->
    </datasource>
    <datasource name="localpostgrestenant"
            helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
            schema-name="public"
            field-type-name="postgres"
            check-on-start="false"
            add-missing-on-start="false"
            use-fk-initially-deferred="false"
            alias-view-columns="false"
            join-style="ansi"
            result-fetch-size="50"
            use-binary-type-for-blob="true"
            use-order-by-nulls="true"
            offset-style="limit">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
        <inline-jdbc
                jdbc-driver="org.postgresql.Driver"
                jdbc-uri="jdbc:postgresql://127.0.0.1:5432/ofbiztenant?sslmode=verify-full&amp;connectTimeout=10&amp;socketTimeout=60&amp;loginTimeout=30&amp;cancelSignalTimeout=10&amp;tcpKeepAlive=true"
                jdbc-username="ofbiz"
                jdbc-password-lookup="localpostgrestenant"
                isolation-level="ReadCommitted"
                pool-minsize="2"
                pool-maxsize="250"
                time-between-eviction-runs-millis="600000"
                pool-sleeptime="20000"
                pool-jdbc-test-stmt="SELECT 1"
                test-while-idle="true"
                test-on-borrow="false"/><!-- Be warned that at this date (2009-09-20) the max_connections parameters in postgresql.conf
                    is set by default to 100 by the initdb process see http://www.postgresql.org/docs/8.4/static/runtime-config-connection.html#GUC-MAX-CONNECTIONS-->
        
        <!-- <jndi-jdbc jndi-server-name="default" jndi-name="java:comp/env/jdbc/localpostgres" isolation-level="ReadCommitted"/>-->
        <!-- <jndi-jdbc jndi-server-name="default" jndi-name="comp/env/jdbc/xa/localpostgres" isolation-level="ReadCommitted"/> --> <!-- Orion Style JNDI name -->
        <!-- <jndi-jdbc jndi-server-name="localweblogic" jndi-name="PostgresDataSource"/> --> <!-- Weblogic Style JNDI name -->
        <!-- <jndi-jdbc jndi-server-name="default" jndi-name="jdbc/localpostgres" isolation-level="ReadCommitted"/> --> <!-- JRun4 Style JNDI name -->
        <!-- <tyrex-dataSource dataSource-name="localpostgres" isolation-level="ReadCommitted"/> -->
    </datasource>

    <datasource name="localoracle"
            helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
            schema-name="OFBIZ"
            field-type-name="oracle"
            check-on-start="true"
            add-missing-on-start="true"
            alias-view-columns="false"
            join-style="ansi"
            use-order-by-nulls="true">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
        <inline-jdbc
                jdbc-driver="oracle.jdbc.driver.OracleDriver"
                jdbc-uri="jdbc:oracle:thin:@127.0.0.1:1521:ofbiz"
                jdbc-username="ofbiz"
                jdbc-password="ofbiz"
                pool-minsize="2"
                pool-maxsize="250"
                time-between-eviction-runs-millis="600000"/>
    </datasource>
    <datasource name="localoracledd"
            helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
            schema-name="OFBIZ"
            field-type-name="oracle"
            check-on-start="true"
            add-missing-on-start="true"
            join-style="ansi"
            use-order-by-nulls="true">
        <read-data reader-name="main"/>
        <inline-jdbc
                jdbc-driver="com.ddtek.jdbc.oracle.OracleDriver"
                jdbc-uri="jdbc:datadirect:oracle://127.0.0.1:1521;SID=ofbiz"
                jdbc-username="ofbiz"
                jdbc-password="ofbiz"
                pool-minsize="2"
                pool-maxsize="250"
                time-between-eviction-runs-millis="600000"/>
    </datasource>

    <datasource name="localsybase"
            helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
            field-type-name="sybase"
            schema-name="dbo"
            check-on-start="true"
            add-missing-on-start="true"
            use-fk-initially-deferred="false"
            join-style="ansi">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
        <inline-jdbc
                jdbc-driver="com.sybase.jdbc2.jdbc.SybDriver"
                jdbc-uri="jdbc:sybase:Tds:10.1.1.10:11222/ofbiz?DYNAMIC_PREPARE=true"
                jdbc-username="ofbiz"
                jdbc-password="ofbiz1"
                isolation-level="ReadCommitted"
                pool-minsize="2"
                pool-maxsize="250"
                time-between-eviction-runs-millis="600000"/>
    </datasource>

    <datasource name="localsapdb"
            helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
            field-type-name="sapdb"
            check-on-start="true"
            add-missing-on-start="true"
            fk-style="name_fk"
            use-fk-initially-deferred="false"
            join-style="ansi-no-parenthesis">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
        <inline-jdbc
                jdbc-driver="com.sap.dbtech.jdbc.DriverSapDB"
                jdbc-uri="jdbc:sapdb://localhost/OFBIZ"
                jdbc-username="ofbiz"
                jdbc-password="ofbiz"
                isolation-level="ReadCommitted"
                pool-minsize="2"
                pool-maxsize="250"
                time-between-eviction-runs-millis="600000"/>
        <!-- <jndi-jdbc jndi-server-name="default" jndi-name="comp/env/jdbc/xa/localsapdb" isolation-level="ReadCommitted"/> --> <!-- Orion Style JNDI name -->
        <!-- <jndi-jdbc jndi-server-name="localweblogic" jndi-name="SapDBDataSource"/> --> <!-- Weblogic Style JNDI name -->
        <!-- <jndi-jdbc jndi-server-name="default" jndi-name="jdbc/localsapdb" isolation-level="ReadCommitted"/> --> <!-- JRun4 Style JNDI name -->
        <!-- <tyrex-dataSource dataSource-name="localsapdb" isolation-level="ReadCommitted"/> -->
    </datasource>

    <datasource name="localfirebird"
            helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
            field-type-name="firebird"
            check-on-start="true"
            use-foreign-key-indices="false"
            add-missing-on-start="true"
            alias-view-columns="false"
            join-style="ansi">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
        <inline-jdbc
                jdbc-driver="org.firebirdsql.jdbc.FBDriver"
                jdbc-uri="jdbc:firebirdsql:127.0.0.1:C:\\data\\ofbiz.gdb"
                jdbc-username="SYSDBA"
                jdbc-password="masterkey"
                isolation-level="ReadCommitted"
                pool-minsize="2"
                pool-maxsize="250"
                time-between-eviction-runs-millis="600000"/>
        <!-- Sample remote URI: jdbc-uri="jdbc:firebirdsql://localhost:3050//opt/interbase/data/ofbiz.gdb" -->
        <!-- <jndi-jdbc jndi-server-name="default" jndi-name="comp/env/jdbc/xa/localfirebird" isolation-level="ReadCommitted"/> --> <!-- Orion Style JNDI name -->
        <!-- <tyrex-dataSource dataSource-name="firebird" isolation-level="ReadCommitted"/> -->
    </datasource>
    <!--
     The following has been tested with SQL Server 2005 + MS SQL Server JDBC Driver 1.1
     Tips:
      1. Make sure your SQL Server has mixed mode authentication as per this post:
       http://aspadvice.com/blogs/plitwin/archive/2006/09/10/Login-failed-_2E002E002E00_-not-associated-with-a-trusted-SQL-server-connection.aspx
      2. Make sure you have copied JDBC driver jar to entity/lib/jdbc
      3. Make sure you have installed JDBC Driver XA support as per MSSQL_JDBC_HOME/enu/xa/xa_install.sql
      4. Make sure that you have created and authorized the ofbiz database and login
      5. Make sure that schema-name, jdbc-username, jdbc-password and databaseName are all correct!

     Notes:
     a. The reason for putting SelectMethod=cursor property in URL is explained here:
      http://forum.java.sun.com/thread.jspa?forumID=48&threadID=184797
     b. If using an old version of the driver, beware of this resource leak:
      http://support.microsoft.com/kb/820773/
     c. Demo data are in conflict with Entity Unique Index when loading data
      This is a known issue with MsSQL since https://markmail.org/message/mezqxb3eyzz3xpv6
      The problem does not exist with H2, nor with the mostly open source DBMS used with OFBiz: Postres, MySQL, MariaDB, etc.
      So we will not change the current OOTB setting and suggest to simply change your own configuration. See OFBIZ-11998 for more.
    -->
    <datasource name="localmssql"
            helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
            schema-name="dbo"
            field-type-name="mssql"
            check-on-start="true"
            add-missing-on-start="true"
            join-style="ansi"
            alias-view-columns="false"
            use-fk-initially-deferred="false">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
        <inline-jdbc
                jdbc-driver="com.microsoft.sqlserver.jdbc.SQLServerDriver"
                jdbc-uri="jdbc:sqlserver://localhost:1791;databaseName=ofbiz;SelectMethod=cursor;"
                jdbc-username="ofbiz"
                jdbc-password="ofbiz"
                isolation-level="ReadCommitted"
                pool-minsize="2"
                pool-maxsize="250"
                time-between-eviction-runs-millis="600000"/>
        <!-- <jndi-jdbc jndi-server-name="default" jndi-name="comp/env/jdbc/xa/localmssql" isolation-level="ReadCommitted"/> --> <!-- Orion Style JNDI name -->
    </datasource>

    <datasource name="localp6spy"
            helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
            schema-name="public"
            field-type-name="postgres"
            check-on-start="true"
            add-missing-on-start="true"
            join-style="ansi">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
        <inline-jdbc
                jdbc-driver="com.p6spy.engine.spy.P6SpyDriver"
                jdbc-uri="jdbc:postgresql://127.0.0.1/ofbiz"
                jdbc-username="ofbiz"
                jdbc-password="ofbiz"
                isolation-level="ReadCommitted"
                pool-minsize="2"
                pool-maxsize="250"
                time-between-eviction-runs-millis="600000"/>
        <!-- <jndi-jdbc jndi-server-name="localjndi" jndi-name="java:/OracleDataSource" isolation-level="Serializable"/> -->
    </datasource>

    <datasource name="localadvantage"
            helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
            field-type-name="advantage"
            check-on-start="true"
            add-missing-on-start="true"
            check-indices-on-start="false"
            use-foreign-keys="false"
            use-foreign-key-indices="true"
            join-style="ansi-no-parenthesis"
            alias-view-columns="false"
            always-use-constraint-keyword="true">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
        <inline-jdbc
                jdbc-driver="com.extendedsystems.jdbc.advantage.ADSDriver"
                jdbc-uri="jdbc:extendedsystems:advantage://localhost:6262;catalog=c:\\advantage\\OFBIZ.ADD;TableType=adt"
                jdbc-username="ADSSYS"
                jdbc-password="adssys"
                isolation-level="ReadCommitted"
                pool-minsize="2"
                pool-maxsize="250"
                time-between-eviction-runs-millis="600000"/>
    </datasource>

    <datasource name="localhsql"
                helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
                field-type-name="hsql"
                check-on-start="true"
                add-missing-on-start="true"
                check-indices-on-start="true"
                use-foreign-keys="true"
                use-foreign-key-indices="true"
                use-fk-initially-deferred="false"
                join-style="ansi-no-parenthesis"
                alias-view-columns="false">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
        <inline-jdbc
                jdbc-driver="org.hsqldb.jdbcDriver"
                jdbc-uri="jdbc:hsqldb:runtime/data/hsqldb/ofbiz"
                jdbc-username="ofbiz"
                jdbc-password="ofbiz"
                isolation-level="ReadCommitted"
                pool-minsize="2"
                pool-maxsize="250"
                time-between-eviction-runs-millis="600000"/>
        <!-- <jndi-jdbc jndi-server-name="localjndi" jndi-name="java:/HsqlDataSource" isolation-level="ReadUncommitted"/> -->
        <!-- <jndi-jdbc jndi-server-name="default" jndi-name="comp/env/jdbc/xa/localhsql" isolation-level="ReadUncommitted"/> --> <!-- Orion Style JNDI name -->
        <!-- <jndi-jdbc jndi-server-name="localjndi" jndi-name="localhsqlDataSource"/> --> <!-- Weblogic Style JNDI name -->
    </datasource>

    <datasource name="localdaffodil"
                helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
                field-type-name="daffodil"
                check-on-start="true"
                add-missing-on-start="true">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
        <!-- this setup is for the embedded version of Daffodil, with in.co.daffodil.db.jdbc.DaffodilDBDriver -->
        <!-- note that Daffodil can also run in a network mode with the driver class: in.co.daffodil.db.rmi.RmiDaffodilDBDriver and URI: jdbc:daffodilDB://<hostname>:<port>/ofbiz;create=true -->
        <inline-jdbc
                jdbc-driver="in.co.daffodil.db.jdbc.DaffodilDBDriver"
                jdbc-uri="jdbc:daffodilDB_embedded:ofbiz;create=true"
                jdbc-username="ofbiz"
                jdbc-password="ofbiz"
                isolation-level="ReadCommitted"
                pool-minsize="2"
                pool-maxsize="250"
                time-between-eviction-runs-millis="600000"/>
        <!-- <jndi-jdbc jndi-server-name="localjndi" jndi-name="java:/DaffodilDataSource" isolation-level="ReadCommitted"/> -->
    </datasource>

    <datasource name="localaxion"
                helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
                field-type-name="axion"
                check-on-start="true"
                add-missing-on-start="true"
                use-pk-constraint-names="false">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
        <inline-jdbc
                jdbc-driver="org.axiondb.jdbc.AxionDriver"
                jdbc-uri="jdbc:axiondb:ofbiz:data/axion/ofbiz"
                jdbc-username="ofbiz"
                jdbc-password="ofbiz"
                isolation-level="ReadCommitted"
                pool-minsize="2"
                pool-maxsize="250"
                time-between-eviction-runs-millis="600000"/>
    </datasource>

    <!-- According to http://markmail.org/message/s75sf6zhtizzkqbv Since version V6R1 (AS/400, db2) there is no need for an own fieldtype.xml - just use the h2-fieldtypes like this: -->
    <!-- Beware use-indices-unique="false" is needed because of H2 bug with null values in a unique index, not sure it's needed with DB2 -->
    <datasource name="DB2"
            helper-class="org.apache.ofbiz.entity.datasource.GenericHelperDAO"
            schema-name="OFBIZ"
            field-type-name="h2"
            check-on-start="true"
            add-missing-on-start="true"
            use-pk-constraint-names="false"
            use-indices-unique="false"
            alias-view-columns="false"
            use-order-by-nulls="true"
            offset-style="fetch">
        <read-data reader-name="tenant"/>
        <read-data reader-name="seed"/>
        <read-data reader-name="seed-initial"/>
        <read-data reader-name="demo"/>
        <read-data reader-name="ext"/>
        <read-data reader-name="ext-test"/>
        <read-data reader-name="ext-demo"/>
            <!--   There is an open source version of the jdbc driver at sourceforge: http://sourceforge.net/projects/jt400/ -->
        <inline-jdbc
            jdbc-driver="com.ibm.as400.access.AS400JDBCDriver"
            jdbc-uri="jdbc:as400:192.168.1.10;prompt=false;libraries=ofbiz;lazyclose=true;translate binary=true"
            jdbc-username="ofbiz"
            jdbc-password="ofbiz"
            isolation-level="ReadCommitted"
            pool-minsize="2"
            pool-maxsize="250"
            time-between-eviction-runs-millis="600000"/>
    </datasource>
</entity-config>
